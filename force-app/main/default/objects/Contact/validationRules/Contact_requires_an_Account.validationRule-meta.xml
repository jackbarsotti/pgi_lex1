<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contact_requires_an_Account</fullName>
    <active>true</active>
    <description>All Contacts must be associated to an Account</description>
    <errorConditionFormula>AND(
ISNULL(AccountId)
)</errorConditionFormula>
    <errorDisplayField>AccountId</errorDisplayField>
    <errorMessage>All Contacts are required to be associated with an Account.</errorMessage>
</ValidationRule>
