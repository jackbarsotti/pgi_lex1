<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Restored_OLA__c</fullName>
    <externalId>false</externalId>
    <formula>IF(ISNULL(Actual_Restore_Time__c),
IF((Restored__c - CreatedDate)*1440*60 - IF(ISNULL(Total_Paused_Time__c),0, Total_Paused_Time__c) &gt; 0,
/* days */
TEXT(FLOOR(((Restored__c - CreatedDate)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0,
Total_Paused_Time__c)))/86400)) +
&quot;:&quot; +
/* hours */
TEXT( FLOOR(MOD(((Restored__c - CreatedDate)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0,
Total_Paused_Time__c))),86400)/3600)) +
&quot;:&quot; +
/* minutes */
TEXT(FLOOR(MOD(((Restored__c - CreatedDate)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0,
Total_Paused_Time__c))),3600)/60)),
&apos;&apos;),
IF((Actual_Restore_Time__c - CreatedDate)*1440*60 - IF(ISNULL(Total_Paused_Time__c),0, Total_Paused_Time__c) &gt; 0,
/* days */
TEXT(FLOOR(((Actual_Restore_Time__c - CreatedDate)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0,
Total_Paused_Time__c)))/86400)) +
&quot;:&quot; +
/* hours */
TEXT( FLOOR(MOD(((Actual_Restore_Time__c - CreatedDate)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0,
Total_Paused_Time__c))),86400)/3600)) +
&quot;:&quot; +
/* minutes */
TEXT(FLOOR(MOD(((Actual_Restore_Time__c - CreatedDate)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0,
Total_Paused_Time__c))),3600)/60)),
&apos;&apos;))</formula>
    <inlineHelpText>Difference between when the case was created and the Service Restored checkbox was checked (minus the time the case was in a Pending Client Action/Info Status)</inlineHelpText>
    <label>Restored OLA</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
