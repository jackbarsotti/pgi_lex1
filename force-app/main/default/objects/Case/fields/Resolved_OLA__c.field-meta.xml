<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Resolved_OLA__c</fullName>
    <externalId>false</externalId>
    <formula>IF((Resolved__c -  Restored__c)*1440*60 - IF(ISNULL(Total_Paused_Time__c),0, Total_Paused_Time__c) &gt; 0, 
/* days */ 
TEXT(FLOOR(((Resolved__c - Restored__c)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0, Total_Paused_Time__c)))/86400)) + 
&quot;:&quot; + 
/* hours */ 
TEXT( FLOOR(MOD(((Resolved__c -  Restored__c)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0, Total_Paused_Time__c))),86400)/3600)) + 
&quot;:&quot; + 
/* minutes */ 
TEXT(FLOOR(MOD(((Resolved__c -  Restored__c)*1440*60 - (IF(ISNULL(Total_Paused_Time__c),0, Total_Paused_Time__c))),3600)/60)), &apos;&apos;)</formula>
    <inlineHelpText>Difference between the time the Service Restored check box was checked and the status was changed to Resolved (minus the time the case was in a Pending Client Action/Info Status)</inlineHelpText>
    <label>Resolved OLA</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
