<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PGi_RecordingType_is_WebOnly</fullName>
    <active>true</active>
    <description>If Recording Type is Web Only other fields are required</description>
    <errorConditionFormula>AND(
$Profile.Name &lt;&gt; &quot;System Administrator&quot;,
NOT(ISCHANGED(IsUpdateFromCaseComment__c)),
ISPICKVAL(Recording_Type__c, &#39;Web Only&#39;) &amp;&amp;
(
ISPICKVAL(Recording_Started_Via__c , &#39;&#39;) 
))</errorConditionFormula>
    <errorMessage>If your Recording Type is Web Only, then you must populate a value in the Recording Started Via field</errorMessage>
</ValidationRule>
