<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PGi_Cant_Change_Owner_from_Marketing_DB</fullName>
    <active>true</active>
    <description>This Account is owned by the Sales &amp; Marketing Database User and only that user may change ownership on this Account..</description>
    <errorConditionFormula>AND(
$User.ProfileId &lt;&gt; &quot;00e30000001kms9&quot;, 
CONTAINS(RecordType.Name, &quot;PGi&quot;), 
$User.Alias &lt;&gt; &quot;autocln&quot;,
PRIORVALUE(OwnerId) = &quot;005a0000009UpsT&quot;,
ISCHANGED(OwnerId),
$User.LastName &lt;&gt; &quot;Marketing Database&quot;,
$User.Override_Validation_Rules__c = False,
NOT($Setup.BypassRules__c.Disable_Validation_Rules__c)
)</errorConditionFormula>
    <errorMessage>This Account is owned by the Sales &amp; Marketing Database User and only that user may change ownership on this Account..</errorMessage>
</ValidationRule>
