<apex:page standardcontroller="Account" showHeader="false" sideBar="false">   
  <apex:includeScript value="https://ajax.googleapis.com/ajax/libs/jquery/1.8.0/jquery.min.js"/>
  <apex:includeScript value="{!URLFOR($Resource.RTResources, 'js/VFResizer.js')}"/>  

  <script>
    jQuery(document).ready(function(){
      VFResizer.resizerProxy = "https://cs13.salesforce.com{!URLFOR($Resource.RTResources, 'html/VFResizerProxy.html')}";
      VFResizer.resize('PlanList');
    });
    /*
    CrossDomainUtility.origin = "https://cs13.salesforce.com";//"https://"+window.top.location.hostname;
    CrossDomainUtility.iframeURL = "{!URLFOR($Resource.RTResources, 'html/CrossBrowserPage.html')}";

    var pageOnLoad = function() { 
      CrossDomainUtility.setHeight(); 
    } 

    CrossDomainUtility.addEvent(window, 'load', pageOnLoad, false);  
    */

    /*
    window.top.addEventListener("click", function(){ console.log('hi'); });

    jQuery.noConflict();

    jQuery(document).ready(function(){ 
      jQuery('input[title="VFResizer"]').css('display', 'none');

      jQuery(window).on('message', function(evt){
        var iframeTitle = evt.originalEvent.data.split(':')[0];
        var newIframeHeight = evt.originalEvent.data.split(':')[1]+'px';
        jQuery('iframe[title="'+iframeTitle+'"]').css('height', newIframeHeight);
      });          
    });
    */
    
  </script>

  <!--
  <iframe src="https://cs13.salesforce.com{!URLFOR($Resource.RTResources, 'html/VFResizerProxy.html')}" height="0px" width="0px"></iframe>
  -->
</apex:page>